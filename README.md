## 2. Greatest and smallest elements
a. Define a (a) method(s) to calculate minimum of given elements.
b. Define a (a) method(s) to calculate maximum of given elements.
i. Take an array from command line and find the maximum and minimum
elements of the given array
ii. Do the same for an array entered by using a Scanner object.
c. Can you define another method that can calculate and return both at the same time?


## 7. String reversed
a. Given a string, reverse it.
b. Define a method that takes a single parameter of type String and returns its reverse.
c. Hint: you may use the method charAt(int index)
d. Note: consider using StringBuilder while generating your resulting reversed string

# 15. Array of invoices
a. Take from the user some information about a few invoices and calculate invoice
amount per each invoice item. Use the Invoice class designed last week.